import React from "react";
import { cleanSourceName, getUrgencyScore, formatDisplayTime } from "../AppHelpers";

export default function StatsPanel({ news, tensionData }) {
  // ...existing code...
}
